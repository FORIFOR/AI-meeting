rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the broker's verified identity and membership checks.
    match /{document=**} { allow read, write: if false; }
  }
}
