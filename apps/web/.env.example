# Optional Google Identity Services browser client ID (public, NOT a client secret).
# Enable Calendar API and register each application origin on an OAuth Web client.
# Without this value the UI explicitly shows calendar integration as unconfigured.
VITE_GOOGLE_CALENDAR_CLIENT_ID=
