# Session telemetry (JSONL)

Numbers-only session reports (`rcai.telemetry.v1`). The broker rejects any payload containing content-like keys (text, transcript, captions, audio, …). Under `strict_local` nothing is sent at all. Files are git-ignored.
