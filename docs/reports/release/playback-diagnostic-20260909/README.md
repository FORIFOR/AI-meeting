# 実Meetの音声再生診断（2026-09-09）

Release = NO_GO。自動試験による実利用評価の代替は行っていない。

## 反映済み

- 未使用agentの状態確認を抑止する修正を公開環境へ反映（Hosting 85463020d98c0808）。
- Botの再生キュー長、AudioContextの動作状態、5秒ごとの音声時計の遅れをheartbeatへ追加（commit 05e2d52、Hosting ee26efa86114e7af）。再生動作自体は変更していない。
- 型検査・728テスト／72ファイル・公開ビルド成功。公開indexとビルドの一致を確認。

## 前回の30分録音を照合

Testerの30分録音（1,800.44秒）を取得し、12件のライブ音声未検出区間を録音の近似対応区間と照合したところ、いずれも閾値を超える音声が検出されなかった。テスター入室時刻を録音開始の近似に使用しており、意味内容や厳密な遅延値を測ったものではない。`previous-recording-check.json`参照。

会議の字幕には、生成済みの返答が十数秒以上遅れて現れる例もある。ただし字幕生成自体の遅れも含むため、字幕の時刻差をそのまま音声到達遅延と断定しない。録音や認証済み録音URLはGitへ保存していない。

## 10分の再診断

専用Meetで主催者のマイク・カメラをオフにし、Yuiと合成音声Testerを使って実行。600,126ms完走。24入力中23件で会議音声を検出。1件は発言判定で抑制、別の予定確認では割り込みが発生したため機能判定はFAIL。無言33,073ms中の新規発言0件。両Botのendedと主催者の退出を確認。音声時計の区間遅れは最大239ms、再生キューは応答後に0へ戻った。今回の未応答時もキューは0であり、今回の無言は再生待ちでは説明できない。前回30分の音声到達問題が解決した証拠ではない。`meet-10min.json`、`playback-timeline.json`参照。Botの返答イベントだけを音声往復の合格とは扱わない。

## 残件

- 音声遅延基準未達（前回30分のp50 1,484ms、p95 1,757ms）。
- 呼びかけ判定で抑制される残存ケースと、会議音声到達のばらつき。
- 規定回数のMeet／実Zoom、実PC2台、15人×3セッションの実利用評価。
- Zoom参加URLまたはログインが必要。診断ブラウザでZoomはサインイン画面だった。
- Hosted追加クレジット購入の承認は未受領。追加購入は行っていない。
