# 最新版の残タスク検証 — 2026-09-09

**Release=NO-GO。** 自動で実施した範囲の検証結果と、実会議・実参加者が必要な未完了項目を分けて記録する。公開設定や合格基準は変更していない。

対象は公開Web `https://ai-meeting.web.app`、broker revision `ai-meeting-broker-00011-swv`、source commit `6c10110`。候補IDはbuild-id.txt。30分試験前後で公開HTMLが一致した。

## 実施結果

| 項目 | 結果 |
|---|---|
| リポジトリ自動テスト | 71ファイル・718件 PASS |
| 公開Web＋実Vertex Gemini連続音声 | 30分完走、画面例外0、HTTPエラー0 |
| 初音遅延 | p50 **1470ms** / p95 **1818ms**、208標本。目標700ms / 1500msを未達 |
| 入力字幕 | 208 / 209発話。件数の指標であり意味認識精度ではない |
| 後続応答 | 206 / 208 = 99.0%。時間順の指標であり回答の正しさではない |
| 割り込み停止 | HUD最大43ms、62標本。物理スピーカー測定ではない |
| 口の停止 | HUD最大0ms、62標本 |
| プロセスメモリー | 途中から30秒ごとに採取。process-memory.json参照。全実機でのメモリー漏れ不存在の証明ではない |
| 通常の実ソケット切断・復旧 | 3 / 3 PASS |
| 10秒通信断の障害注入・復旧 | 3 / 3 PASS |
| 10秒間の429注入・復旧 | 3 / 3 PASS（約15.5〜16.3秒後にready） |
| 10秒間のタイムアウト注入・復旧 | 3 / 3 PASS（約16.3秒後にready） |
| 退出後のイベント停止・新規会話のハンドル分離 | 上記ライフサイクル試験で確認 |
| 5種類の声 | 各1発話で音声生成・PCM欠損/非数/クリッピング0・予定の終了時刻を正答 |

通信障害試験は、実Geminiソケットを切断し、トークン取得部分の通信・応答を制御している。物理ネットワークやBluetoothを切断した試験ではなく、復旧とは実際の再開ハンドルで接続がreadyになったことを指す。音響品質の人による採点ではない。

## 試験設定と追加観測

30分試験のconsoleエラーは184件あり、元のFAIL判定を保持する。ランナーが公開版に対してローカル補助サーバー `ws://localhost:8788` を明示設定し、10秒ごとの確認で接続拒否が出ていた。通常の公開版既定値を指定した2分の切り分け試験ではconsole・画面・HTTPエラー各0件を確認し、default-agent-smoke/に保存した。ただし入力字幕件数と標本不足により短い試験全体の判定はFAILであり、品質合格とはしない。短い再試験で30分全体を合格に読み替えない。

5声の試験では、同じ合成入力の終了より先に応答音声を受信し、最後の丁寧な依頼の部分が最終入力字幕に現れなかった。負の値を「速い遅延」として合格に使わない。voice-assessment.jsonに限定された観測として記録した。

## 未完了

実Meet/Zoomは各30分10回・60分3回・実機2台の現候補の証拠が不足。15人×3回の本人による評価も未受領。現在使える会議URL、ホストの承認・操作、実機担当者、実参加者または非公開個票の保存先をユーザーに確認中。今回、新しい会議Botは作成していない。

participant-guide.md と evaluation-slots.csv は案内と空欄の記録表であり、評価結果ではない。open-items.md に未達・実行条件をまとめた。現候補の公開判定は **6 PASS / 2 FAIL / 42 BLOCKED**。過去版のMeet入室成功やAIテスターを、人による評価として代用していない。

## 再実行時の接続設定

公開版の通常設定に合わせ、クラウド音声試験では `--agent wss://ai-meeting.web.app/agent` を明示する。ランナーの既定値 `ws://localhost:8788` はローカル構成用である。比較元の30分試験はこの既定値を使ってしまったため、consoleエラーを除外・書き換えずに保存した。公開版の機能・設定や認識の待ち時間を変更して数値だけ改善したものではない。
