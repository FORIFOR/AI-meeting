# AI-meeting TODO — 高性能を維持して原価を下げ、公開可能にする

更新: 2026-09-08。現状は **Release=GO未達**。優先順位は速度・回答品質・安定性を満たしたうえで原価削減。試験運用はAttendee Hosted + Gemini 3.1 Flash Live + 小規模クラウドを採用。通常5,000円/月、上限10,000円/月。詳細は[Hosted試験運用](../../deploy/cloud/hosted-pilot.md)。

## 完了済み（合格・公開済みという意味ではない）

- [x] 性能再評価コマンド `pnpm performance:assess <soak.json>` を追加。
- [x] 遅延・聞き取り率・応答率・エラー・必要サンプル数をsoak判定へ組み込み。
- [x] ループ音声の入力発話数と応答の重複カウントを修正。
- [x] 公開判定の速度条件を強化。型チェック・ビルドを実行。
- [x] 保存済みGemini/local実行記録を再評価し、旧PASSが新基準ではFAILになることを確認。

これらの最新変更は作業ツリーにあり、コミット・GitHub反映は未実施。保存済みのGemini接続・再開成功記録はあるが、新しい候補での再検証を代替しない。

## P0 — 計測を正し、速度と品質の基準を満たす

| ID | TODO | 完了条件 | 依存・必要環境 |
|---|---|---|---|
| P0-1 | 実音声の遅延計測を整備 | 発話終了→API初音→PC再生→相手側で聞こえるまでを区別。無応答・20秒超などの除外件数も報告し、遅い応答を分母から隠さない | 実マイク、参加側端末、Meet/Zoom試験会議 |
| P0-2 | 判定指標を統一 | HUD最大値と公開判定p95、音声入力とテキスト入力の違いを明記。発話対応付けと欠測処理を実記録で照合 | P0-1 |
| P0-3 | 評価APIの502を調査・修正 | `/api/evaluate`の再現条件・原因を特定し、実接続で正常完了。失敗時にも結果を成功扱いしない | バックエンドログ、利用中API |
| P0-4 | 応答遅延を改善 | 実会議の意味ある初音までp50 ≤ 700ms、p95 ≤ 1500ms、100発話以上。端末・回線・モデル条件を記録 | P0-1、P0-2 |
| P0-5 | 割り込み確認待ちを改善 | ユーザー発話開始→停止 ≤ 200msを目標に測定。停止要求→停止の最大値 ≤ 150ms、20件以上。誤割り込み率 < 1%も満たす | 現Botの600ms確認待ちを調査。実音声の割り込み・相づち |
| P0-6 | 聞き取り・回答品質を検証 | 聞き取り/応答の件数近似98%以上に加え、意味内容を照合。意図認識エラー < 2%、文脈の取り違え・架空の記憶・決定事項の捏造を検出 | 実会話と人間による評価 |
| P0-7 | 記憶・長時間品質を改善 | 60分会議で決定事項・担当者・期限・否定済み前提を照合。保存済み会話試験のプロジェクト想起失敗も再確認 | P0-6 |

## P1 — 品質を維持した原価削減と配置判断

| ID | TODO | 完了条件 | 依存・必要環境 |
|---|---|---|---|
| P1-1 | 実原価を可視化 | 音声/テキスト/映像/検索等の利用量、Bot稼働、保存・転送を区別。請求と突合し会議1時間あたり原価を算出 | 実API利用量・請求情報 |
| P1-2 | 重複処理・不要送信を削減 | 不要映像、重複文字起こし、重複AI呼び出し、待機ワーカーを調査し、存在する無駄だけ除去。削減前後でP0が悪化しない | P0、P1-1 |
| P1-3 | PC音声方式とAttendee方式を比較 | 同じ利用条件で速度・品質・CPU/メモリ・原価・導入負担を比較。PC方式は会議側への音声出力とエコー抑制まで確認 | 対象PC、実会議 |
| P1-4 | 提供構成を決定 | PC主体/クラウドBot/併用を測定結果で選択。対応OS、最低端末性能、同時接続上限、API費用負担、時間あたり原価目標を決定 | P1-1〜3、事業上の予算・対象ユーザー |
| P1-5 | コスト上限を実装 | 同時実行・利用時間・API利用量の上限と監視。会議の途中で無言停止せず、上限到達を利用者へ明示 | P1-4 |

## P2 — 配布・運用・公開リリース

- [ ] クローン先の実パス、`.env`転送、Attendee到達、音声/Webhook往復を確認。現状の別PC移行は完了未確認。
- [ ] 選んだ構成で導入・更新・復旧手順を整備。PC配布なら署名・権限・音声経路、クラウドならHTTPS・永続化・監視・バックアップを確認。
- [ ] 認証、ユーザー間データ分離、APIキー保管、利用量制限、データ保存/削除を実装状況から監査し、不足を修正。運営のキーを配布物へ含めない。
- [ ] 実Gemini再接続、ネットワーク断、レート制限、デバイス切替、退出/キックを再検証。重複音声・退出後音声・別会議/別ユーザー混入ゼロ。
- [ ] 公開対象プラットフォームごとに30分×10回、60分×3回、物理PC2台以上で安定性を検証。
- [ ] 一般ユーザー15人以上・各3セッション以上で自然さ/有用性/再利用意向などを評価。公開対象モードも個別に評価。
- [ ] 公開対象モード/プラットフォームを先に明示し、対象すべての証拠を収集。現在beta/disabledのため条件付きゲートが出ない状態を、要件達成とみなさない。
- [ ] 変更をレビューし、適切なチェックを通してコミット・GitHubへ反映。現在の候補buildIdに対応する証拠・ハッシュ・測定日時・評価者を揃える。
- [ ] `pnpm release:reality`で公開対象の全ゲートを確認。FAIL/BLOCKEDを解消し、証拠に基づいてRelease=GOを判定する。

## 実行順

P0-1/2（計測）→ P0-3〜7（修正と実会議検証）→ P1（原価比較・構成決定）→ P2（運用・配布・公開）。現行実装の監査と原価計測の準備は並行可能。

実会議URL・参加端末・評価参加者・請求情報などが必要な段階では、具体的な不足情報を整理する。ダミーや合成音声を人間の実会議評価の代わりにしない。

参照: [改善方針](quality-first.md)、[公開判定スクリプト](../../scripts/release/reality.mjs)、[公開範囲ポリシー](../../packages/conversation-core/src/release-policy.json)。
