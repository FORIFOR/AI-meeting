# Gemini Live — 実機ゲート報告

対象: `gemini-3.1-flash-live-preview` / `thinkingLevel: minimal` / 音声 `Achird`
期間: 2026-09-07 · ブランチ `release/0.2.0-beta.1` · 実施 run 117–119 + P0 計測

---

## 1. 結論

一般ユーザ向けの P0 5項目は**すべて PASS**。会議室なしで再現・再計測できる形になっている。
実 Google Meet では「ハウリング」「返事がない」「共有していない資料の話」の3つが順に出て、
いずれも原因を特定して修正済み。ニュース・天気は実際に答えられるようになった。

| P0 | 判定 | 実測 |
|---|---|---|
| ① 勝手に喋らない | PASS | 6秒の無音に発話なし。会議ペルソナでは呼びかけのない発言のあとも `OBSERVING` |
| ② 呼んだらすぐ答える | PASS | 3/3 応答・初音 p50 **121 ms**（別測定で 203–360 ms） |
| ③ 人が喋ったら止まる | PASS | 割り込みから **265 ms** で停止。相手の発話中に部屋へ届いた音声 0 フレーム |
| ④ 短く自然に喋る | PASS | 1発話 3.3–9.6 秒（上限12秒） |
| ⑤ 聞き取れなければ聞き返す | PASS | 断片「それっ？」に対し「「これ」って、さっき話そうとしてた…のこと？」と聞き返す |
| API コスト | INFO | 部屋の音声の **20–28%** だけを送信（VADゲート） |

再現: `services/agent/node_modules/.bin/tsx scripts/reality/p0-gate.ts <録音ディレクトリ>`

---

## 2. 実会議で出た不具合と原因

### 2.1 返事がない（run 117）
ページのカウンタは `文字起こし 48 / 応答 0`。**ターンが終わっていなかった**。
音声ゲートのフォールバックが -45 dBFS の固定しきい値で、自動ゲイン付きの会議音声は無音時でも
これを超えるため、ゲートが開いたまま `activityEnd` が送られず、モデルは「ユーザーの発話が終わった」
と知らされないまま文字起こしだけを返していた。

修正: 判定を VAD が学習するノイズフロアからの相対値に変更。ターンは最大8秒で強制クローズ。

### 2.2 返事がカットされる（run 119前）
`応答開始 14 / カット 8 / 文字起こし 4`。Gemini はユーザーの確定文字起こしを **自分の返答終了時**
に返すため、返答開始時点でページに判断材料がなく `unsanctioned` として毎回切っていた。

修正: ゲートがユーザーのターンを閉じた時点で文字起こしを確定。さらに文字起こしが無くても
「相手の声が聞こえた」だけでターンを認める（1対1のみ）。

### 2.3 ハウリング
harness が彼女の声をリレー経由でも送信し、Attendee の webpage streamer がページ音声をマイクとして
拾う経路と二重になっていた。0.5秒ずれた2つの声が互いを増幅。

修正: 出力経路を `page` 単一に。証拠行はページ側の発話フレーム数を読む。

### 2.4 共有していない資料の話
指示文がすべて「会議」前提で、議事録・資料の例ばかりだった。会議のない場ではそれを作り出す。

修正: `settingFor` で1対1と会議を分離。1対1には「会議ではありません」「見ていないものを見たことに
しない」「相手が出していない資料の話を自分から始めない」を明示。ペルソナも `friend_ja` に変更。

### 2.5 6秒の無音に返事をする
VAD のノイズフロア初期値が -60 dBFS で、それより大きい部屋は最初のフレームから「音声」と判定される。

修正: セッション開始後 400 ms は判定せずフロアを実測して設定（-45 dBFS で上限）。
ゲートのフォールバックはその間開かない。

---

## 3. ニュース・天気

Gemini Live の検索グラウンディングは**このキーでは 3.1 に対して拒否される**。ツールを宣言した瞬間に
ソケットが 1011（quota超過）で閉じるため、そのまま入れると全セッションが接続不能になる。

| モデル | 初音 p50 | 検索 |
|---|---|---|
| gemini-3.1-flash-live-preview | 922 ms | 宣言すると接続拒否 |
| gemini-2.5-flash-native-audio-latest | 3 251 ms | 動作（実際の今日のニュースを回答） |

2.5 は3.5倍遅いうえ、共有していない資料に「はい、拝見しました」と答え、架空の予定も作った。
3.1 は同じ質問に「まだ見てないんだ」と正直に答える。したがって会話用は 3.1 のまま。

代わりに**鍵のいらない公開ソースを引くツール**を持たせた（`lookup_live_info`）。
Google ニュースの公開RSSと Open-Meteo。ブローカー側で実行し、彼女は返ってきた事実を読み上げるだけ。

実際の応答:
- 「今日のニュースを教えて」→「伊豆諸島の方で大雨の警戒レベルが出てるみたい。あと、政治の動きも色々あるよ。」
- 「今日の東京の天気は？」→「今日の東京は強い雨が降る予報だよ。最高でも24度だって。お出かけするなら雨具が必須だね。」

検索グラウンディングを使う場合は、キー側で枠を有効にする必要がある。

---

## 4. 声

既定を Kore（Google 分類 "Firm"）から **Achird（"Friendly"）** に変更。
比較候補は Achird / Leda / Sulafat。ユーザーに見せる選択肢は日本語のみ
（親しみやすい・明るい・やさしい・ていねい・落ち着いた）で、Google のボイス名は出さない。
ブラインド用サンプル生成: `scripts/reality/voice-samples.ts`

---

## 5. 残っている作業

- 実 Google Meet での 15〜20 ターン通し確認（P0はオフラインで PASS 済み、未実施なのは人の判断部分）
- 3音声ブラインド比較の結果反映
- Level 1 Private Assist（本人だけに出す提示）、Level 3 Facilitate
- 会議後のまとめ・アクション抽出
